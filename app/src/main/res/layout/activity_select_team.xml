<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:orientation="vertical"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".SelectTeam">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select squad from list below or click here to select team from all Athletes"
        android:id="@+id/selectTeamBtn"
        android:layout_centerHorizontal="true"
        android:layout_gravity="center_horizontal" />

    <LinearLayout
        android:layout_height="match_parent"
        android:layout_width="match_parent"
        android:orientation="vertical">

        <ListView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/squadListView"
            android:layout_below="@+id/selectTeamBtn"
            android:layout_alignParentEnd="true"
            android:layout_weight="2"
            android:layout_alignParentStart="true" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="2.4"
            android:id="@+id/teamTextView"
            android:gravity="center_horizontal|bottom"
            android:layout_marginBottom="4dp"
            android:textColor="#000"
            android:textStyle="bold" />

        <ListView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/teamListView"
            android:layout_below="@+id/squadListView"
            android:layout_alignParentEnd="true"
            android:layout_weight="1"
            android:layout_alignParentStart="true" />

    </LinearLayout>

</LinearLayout>
